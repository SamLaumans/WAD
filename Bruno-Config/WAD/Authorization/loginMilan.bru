meta {
  name: loginMilan
  type: http
  seq: 5
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
      "username": "Milan",
      "password": "Testing456"
  }
  
}

script:post-response {
  const data = res.getBody();  
  bru.setVar("token", "Bearer " + data.token);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
